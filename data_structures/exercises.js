/* Iteration
 * https://repl.it/data/classrooms/share/5b476151afc7998bac7715a7d0dbe0a2
 * Test your results: npm run test
**/

/**
 * Given an array of numbers, double each number and return a new array of the results
 * Ex. double([1,2,3]) returns: [2, 4, 6]
 */
function double(arrayOfNumbers) {
    // code here
}

/**
 * Given a phrase return an array that includes it n times
 * Ex. repeat('hi', 3) returns: ['hi', 'hi', 'hi']
 */
function repeat(phrase, n) {
    // code here
}

/**
 * Return the index of str within arr
 * @param str - a string
 * @param arr - an array of strings
 */
function findPosition(str, arr) {
    // code here
}

/**
 * Return an object mapping each key to each value
 * @param keys - an array of strings
 * @param values - an array of data
 */
function zip(keys, values) {
    // code here
}

module.exports = {
    double: double,
    repeat: repeat,
    findPosition: findPosition,
    zip: zip
};
